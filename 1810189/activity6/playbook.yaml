---
- name: Installing dhcp, bind9, vsftpd, samba, http, mariadb in ubuntu
  hosts: ubuntu

  roles:
    - Installing_Ubuntu

- name: Installing dhcp, bind9, vsftpd, samba, http, mariadb in centos
  hosts: centos

  roles:
    - Installing_Centos
